{
    "webPort": 8080,

    "numChildren": 1,

    "params": {
        "appName": "Formula 1"
    },

    "cors": {
      "origin": true,
      "credentials": true,
      "headers": [ "x-requested-with", "content-type", "X-XSRF-TOKEN" ],
      "methods": [ "GET", "PUT", "POST", "DELETE", "OPTIONS" ],
      "maxAge": 1000000000
    }
}